# 蓝鸟平台：第一批验收与第二批实现记录

日期：2026-09-23。基于当前工作区；未提交、未部署。保留本轮开始前已有的界面改动。

## 第一批

已实现并检查：

- 首页“继续已有棋谱”优先，导入可折叠，棋盘只有主动进入 Explore a position 才成为编辑入口。
- 原创蓝鸟与羽毛 SVG；笔记保存反馈使用短暂羽毛落定动画。
- 复盘开始、关键时刻、练习模式的棋盘布局固定；切换面板使用 240ms 动画，连续切换会取消旧动画，不重新挂载棋盘。
- 1280×720 下开始/关键时刻/练习棋盘位置相同：x=255、y=202.75、390×390。
- 320 与 390 像素宽度的第一批复盘页已检查：长姓名换行、准确率不压住工具栏、评价数字不拆行，页面无横向溢出。
- 390 像素首页检查：继续入口和导入入口可见，无横向溢出。
- 通过真实笔记表单保存临时书签，保存提示和羽毛可见；本轮临时记录已移除。

边界：继续入口指向最近保存的棋谱，不表示已实现“恢复上次浏览到的具体一步”。使用独立本地端口 3001 的空库实测：导入默认展开，PGN 草稿折叠后重新展开仍完整保留，未清空现有数据。

## 第二批

| 问题 | 已实现修复 | 验收 |
|---|---|---|
| 工具页缺少共同的构图语言 | FolioHeading、书脊细线、衬线标题、页边羽毛 | Library / Stats / Settings 桌面截图检查 |
| 棋谱文字直接压在背景上 | 连续纸页，修正透明背景覆盖规则，长标题换行 | 106 条记录的真实库首屏检查 |
| 统计纵向堆叠 | 桌面双栏、窄屏单栏，等宽数字 | 桌面真实库检查 |
| 设置页太长，入口不清楚 | 六项页内目录、44px 主要输入控件 | 实际点击 Backup & data 到达 Local data |
| 笔记正文受背景干扰 | 实色纸面和内边距 | 真实 Notebook 画面检查 |
| 收尾缺少平台记忆点 | 满足全部关键时刻已浏览时播放 600ms 蓝鸟 | 实际走过 5 个时刻，再 Finish review，飞鸟可见 |
| 提前结束也被标为完成 | Review summary + 原有计数，无飞鸟 | 0/5 提前结束，飞鸟节点数为 0；完整结束为 1 |

初次验收时（现已补验，见文末）第二批手机端验收尚未通过：浏览器视口工具调用后 DOM 仍报告 1280px，不能将其作为 320/390px 验收证据。已实现响应式规则，但需有效窄屏环境复验。系统减少动态效果分支已代码检查，未切换系统设置进行实测。

## 检查

- `pnpm --filter @chess-review/web test`：53 文件、409 测试通过。
- 针对本批 TSX 与第一批主要 TSX 的 ESLint 通过；不是全仓库 lint 结论。
- `pnpm --filter @chess-review/ui typecheck` 通过；`pnpm --filter @chess-review/web build` 通过，包含 Web TypeScript 检查及 27 个页面构建。
- 检查最终 diff；本轮无新增生产依赖、无新增位图大文件、无分析算法修改。

## 后续验收优先级

1. 第二批 Library / Stats / Settings / Notebook / 完成总结在 320、390、1024 宽度下复验。
2. 键盘焦点顺序专项验收（空库与导入草稿折叠保留已通过）。
3. 减少动态效果的实际系统环境验收。
4. 后续再扩展路由级衔接与专属空状态插画；本轮没有引入全屏开场或滚动接管。

本记录确认限定范围的实现与检查，不代表完整方案已经完成，也不代表平台已全面达到 Chess.com / Lichess 的功能成熟度。

## 第三批与第二批补验（同日后续）

### 已实现

- 跨页面 240ms 轻入场；同一盘棋的子路由不移动整个棋盘，减少动态效果时禁用。
- 路由导航焦点落到首个 h1，新增 Skip to content；手机菜单离焦关闭、Escape 返回 Menu。
- 空 Library / Review / Stats 使用原创蓝鸟和真实导入链接；无搜索结果提供 Clear filters，不改动数据。
- 320px 棋谱记录的标题、打开和删除按钮分行；完成总结标签与内容改为上下排列，修复图标挤压正文的问题。

### 真实浏览器验收

本轮视口工具已正常应用（DOM 报告 320/390/1024，而非旧的1280）：

- Library：390、320、1024；320 下长对局名换行，打开/删除可独立点击，无横向溢出。
- Settings：390、320、1024；320 下点击 Backup & data 到达完整备份区。
- Stats：320 单栏、1024 双栏。
- Notebook：320 下编辑框、保存按钮、书签复选框完整可达，scrollWidth=innerWidth=320。
- 完成总结：320 下实际走过5个关键时刻并 Finish review；修复后标签与证据完整显示。
- 三个真正空库页面：独立端口3001的生产预览，320px实测；修复 Library 主按钮被普通链接色覆盖的问题。
- 菜单：打开聚焦 Home，Escape 返回 Menu。Stats → Open library 导航后 activeElement 为 Games and reviews 标题。
- 筛选：输入不存在的搜索词得到 No matching games，Clear filters 恢复106条记录。

第二批窄屏工具阻塞已解除；以上是实际覆盖的尺寸，不声称全部页面都覆盖所有尺寸。减少动态效果已实现并代码检查，仍未切换系统设置实测。

### 工程检查

本批 Web 53文件/409测试通过，修改的 TSX 定向 ESLint 通过；生产构建（含TypeScript）通过。无分析算法、计分、引擎协议或存储格式变更。
