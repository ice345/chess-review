# Fill in ONLY the hostname, without https:// or a path.
DOMAIN=
PORT=8080
# Optional. Use a unique public OAuth client ID, e.g. your hostname, to enable Lichess.
LICHESS_CLIENT_ID=
# release.py init generates this once. Preserve it across upgrades.
LICHESS_SESSION_SECRET=
