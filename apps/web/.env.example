# Public OAuth client identifier. Use a unique deployment hostname or app ID.
LICHESS_CLIENT_ID=open-chess-review.example.com

# Private server-only key used to encrypt HttpOnly OAuth session cookies.
# Generate locally, for example: openssl rand -base64 32
LICHESS_SESSION_SECRET=replace-with-at-least-24-private-characters

